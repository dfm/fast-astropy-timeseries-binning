import numpy as np

# from fast_astropy_timeseries_binning._fast_astropy_timeseries_binning import reduceat
from fast_astropy_timeseries_binning.version import __version__ as __version__


# class reducer:
#     @staticmethod
#     def reduceat(array, indices):
#         out = np.empty(indices.shape, dtype=array.dtype)
#         reduceat(array, indices, out)
#         return out
